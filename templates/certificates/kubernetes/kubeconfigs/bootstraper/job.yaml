apiVersion: batch/v1
kind: Job
metadata:
  name: kubeconfigs-bootstraper
spec:
  backoffLimit: 2
  template:
    spec:
      containers:
      - name: kubeconfigs
        image: {{ .Values.registry }}/{{ .Values.repository }}/kubeconfigs:{{ .Chart.AppVersion }}
      restartPolicy: Never
      serviceAccountName: kubeconfigs-bootstraper
