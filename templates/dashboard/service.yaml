apiVersion: v1
kind: Service
metadata:
  name: dashboard
spec:
  type: ClusterIP
  ports:
  - name: https
    protocol: TCP
    port: 8443
    targetPort: 8443
  selector:
    app: kubernetes-dashboard